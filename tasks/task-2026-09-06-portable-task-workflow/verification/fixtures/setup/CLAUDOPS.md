# Claudops project context

Task root: work-items/
Command sources: package.json (`npm test`, `npm run lint`)
Project constraints: Before working with an API, read `docs/product/` when that directory exists.
